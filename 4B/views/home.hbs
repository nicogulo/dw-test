{{#each rows}}
  <div class="">

  </div>

  {{!-- <div class="container">
    <div class="row">
      <div class="col-8">
        <div class="card" style="width: 18rem;">
          <img src={{this.image}} class="card-img-top" alt={{this.name}} />
          <div class="card-body">
            <h5 class="card-title">{{this.name}} </h5>
            <h5 class="card-title">Stok {{this.stok}} </h5>
            <p class="card-text">{{this.deskripsi}} </p>
            <a
              href="/viewbooks/{{this.id}}"
              type="button"
              class="btn btn-light btn-small"
            ><i class="bi bi-eye"></i> View</a>
            <a
              href="/editbooks/{{this.id}}"
              type="button"
              class="btn btn-light btn-small"
            ><i class="bi bi-pencil"></i>
              Edit</a>
            <a
              href="/{{this.id}}"
              type="button"
              class="btn btn-light btn-small"
            ><i class="bi bi-person-x"></i>
              Delete</a>
          </div>
        </div>
      </div>

    </div>
  </div> --}}

  {{!-- <div>
        <img src={{this.image}} alt="" />
        <p>{{this.name}} <span>stok {{this.stok}}</span></p>
        <a
          href="/viewbooks/{{this.id}}"
          type="button"
          class="btn btn-light btn-small"
        ><i class="bi bi-eye"></i> View</a>
        <a
          href="/editbooks/{{this.id}}"
          type="button"
          class="btn btn-light btn-small"
        ><i class="bi bi-pencil"></i>
          Edit</a>
        <a href="/{{this.id}}" type="button" class="btn btn-light btn-small"><i
            class="bi bi-person-x"
          ></i>
          Delete</a>
      </div> --}}

  <div class="container-fluid">
    <div class="row">
      <div class="col-12 mt-3">
        <div class="card">
          <div class="card-horizontal d-flex">
            <div class="img-square-wrapper">
              <img src={{this.image}} alt={{this.name}} />
            </div>
            <div class="card-body">
              <h4 class="card-title">{{this.name}}</h4>
              <p class="card-text">{{this.deskripsi}}</p>
              <form action="/" method="POST" onsubmit="disableButton()">
                <button
                  id="btn"
                  type="submit"
                  class="btn btn-primary"
                >Pinjam</button>
                <button
                  id="btn"
                  type="submit"
                  class="btn btn-warning"
                >Kembalikan</button>
              </form>
            </div>
          </div>
          <div class="card-footer">
            <small class="text-muted">Stock {{this.stok}}</small>
            <br />

            <a
              href="/viewbooks/{{this.id}}"
              type="button"
              class="btn btn-light btn-small"
            ><i class="bi bi-eye"></i> View</a>
            <a
              href="/editbooks/{{this.id}}"
              type="button"
              class="btn btn-light btn-small"
            ><i class="bi bi-pencil"></i>
              Edit</a>
            <a
              href="/{{this.id}}"
              type="button"
              class="btn btn-light btn-small"
            ><i class="bi bi-person-x"></i>
              Delete</a>
          </div>
        </div>
      </div>
    </div>
  </div>

  {{!-- <tr>
    <th scope="row">{{this.id}}</th>
    <td> <img src={{this.image}} alt={{this.name}} /></td>
    <td>{{this.name}}</td>
    <td>{{this.stok}}</td>
    <td>{{this.deskripsi}}</td>
    <td class="text-end">
      <a
        href="/viewbooks/{{this.id}}"
        type="button"
        class="btn btn-light btn-small"
      ><i class="bi bi-eye"></i> View</a>
      <a
        href="/editbooks/{{this.id}}"
        type="button"
        class="btn btn-light btn-small"
      ><i class="bi bi-pencil"></i>
        Edit</a>
      <a href="/{{this.id}}" type="button" class="btn btn-light btn-small"><i
          class="bi bi-person-x"
        ></i>
        Delete</a>
    </td>
  </tr> --}}

{{/each}}

{{! <table class="table table-bordered">
  <thead class="thead-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Preview</th>
      <th scope="col">Name</th>
      <th scope="col">Stok</th>
      <th scope="col">Description</th>
      <th scope="col" class="text-end">Action</th>
    </tr>
  </thead>
  <tbody>

  </tbody>
</table> }}